<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.att.repository.mapper.AttDetaileMapper">
  <resultMap id="BaseResultMap" type="com.att.repository.entity.AttDetaile">
    <result column="SITE" jdbcType="VARCHAR" property="site" />
    <result column="ITEM" jdbcType="VARCHAR" property="item" />
    <result column="OPERATION" jdbcType="VARCHAR" property="operation" />
    <result column="DPSOBJECT" jdbcType="DECIMAL" property="dpsobject" />
    <result column="MPSOBJECT" jdbcType="DECIMAL" property="mpsobject" />
    <result column="PRODUCT_DAY" jdbcType="VARCHAR" property="productDay" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    SITE, ITEM, OPERATION, DPSOBJECT, MPSOBJECT, PRODUCT_DAY
  </sql>
  <select id="selectByExample" parameterType="com.att.repository.entity.AttDetaileExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from ATT_DETAILE
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <select id="countByExample" parameterType="com.att.repository.entity.AttDetaileExample" resultType="java.lang.Long">
    select count(*) from ATT_DETAILE
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
</mapper>